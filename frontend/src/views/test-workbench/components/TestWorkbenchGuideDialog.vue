<template>
  <GuideDialog
    v-model="visible"
    title="测试工作台使用指南"
    intro="按“系统与版本 → 需求 → 用例 → 执行 → Bug”的顺序了解测试工作台；也可以通过左侧目录直接查看各模块的操作说明。"
    :sections="sections"
  />
</template>

<script setup>
import { computed } from 'vue'
import GuideDialog from '@/components/GuideDialog.vue'

const props = defineProps({ modelValue: { type: Boolean, default: false } })
const emit = defineEmits(['update:modelValue'])

const visible = computed({
  get: () => props.modelValue,
  set: value => emit('update:modelValue', value),
})

const sections = [
  {
    key: 'start', index: '1', label: '快速上手',
    summary: '以项目为入口，先确定系统和版本范围，再依次完成需求、用例、执行和 Bug 闭环。',
    steps: ['在系统管理中创建系统，再在对应系统下创建版本。', '在左下角选择当前系统和版本；需求、用例、Bug、遗留项都会按该范围展示和管理。', '录入原始需求；需要澄清时可使用 AI 补全，也可将多条已确认需求合并。', '需求确认后才能作为 AI 生成用例的来源；用例也可以手工创建。', '检查并确认用例后执行测试；执行失败时可直接提交 Bug，并跟踪处理和验证结果。'],
    flowTitle: '核心流程',
    flow: ['系统', '版本', '原始需求', '补全 / 合并', '确认需求', '生成用例', '执行测试', 'Bug 闭环'],
    tip: 'AI 生成测试用例仅可使用当前系统、当前版本下已确认的需求；手工创建用例不受此限制。',
  },
  {
    key: 'scope', index: '2', label: '系统与版本',
    summary: '系统和版本用于划分测试资产范围，避免不同产品线或迭代的数据混在一起。',
    steps: ['在系统管理中新建系统，填写名称、类型和描述。', '在版本管理中为系统建立版本，并记录当前阶段和发布日期。', '通过左下角“编辑”切换当前系统和版本；也可以选择全部范围查看汇总数据。'],
    tip: '系统和版本本身没有状态流转；版本阶段仅用于记录当前迭代情况。',
  },
  {
    key: 'requirements', index: '3', label: '需求管理',
    summary: '维护原始需求，并通过 AI 追问补全，或将多条需求合并为统一口径的正文。',
    steps: ['在原始需求中新建或编辑正文；内容完整后即可确认。', '需要澄清时，从原始需求或 AI 补全需求列表发起 AI 补全；追问结束后预览并保存。', '在原始需求或 AI 补全需求列表中，选择同一系统、同一版本下的已确认需求，发起 AI 合并并保存。', '原始需求、AI 补全需求和 AI 合并需求都要在各自列表中确认后，才能用于 AI 生成用例。'],
    flow: ['待确认', '已确认'],
    flowDescription: '三类需求独立保存、独立确认，状态互不影响。只有已确认需求才能作为 AI 生成测试用例的来源。',
    tip: '需求正文支持 Markdown；查看、编辑和复制均保留原始正文格式。',
  },
  {
    key: 'cases', index: '4', label: '用例管理',
    summary: '基于已确认的原始需求、AI 补全需求或 AI 合并需求生成可执行用例，也支持手工新建、编辑和确认。',
    steps: ['AI 生成时，选择当前系统、当前版本下同一种已确认需求类型；可以多选同类需求。', 'AI 会先追问影响执行的必要疑点，确认后生成用例预览；在预览中勾选并保存需要的用例。', '也可以手工新建用例，关联需求为可选。', '在待确认列表检查内容后确认用例；只有已确认用例可以执行。'],
    flow: ['待确认', '已确认'],
    flowDescription: '用例确认状态与执行状态分开管理。执行结果只保留未执行、通过、失败三种状态。',
    tip: '系统会分配不可编辑的用例编号；执行失败时可直接携带用例信息提交 Bug。',
  },
  {
    key: 'bugs', index: '5', label: 'Bug 管理',
    summary: '记录执行中发现的问题，明确处理人和验证人，并通过修复与验证形成闭环。',
    steps: ['手工新建 Bug，或从失败的测试用例直接创建并自动带入相关信息。', '填写严重级别、优先级和处理人；也可以先提交，再补充指派。', '处理人标记已解决并填写解决方案，测试人员验证通过后关闭；所有流转记录都可在详情中查看。'],
    flow: ['待解决', '已解决', '已验证关闭'],
    flowDescription: '验证不通过必须填写原因，Bug 将重新激活并回到待解决。',
    tip: 'Bug 列表用于完整管理；待办事项用于处理自己当前需要修复或验证的 Bug。',
  },
  {
    key: 'legacy', index: '6', label: '遗留项管理',
    summary: '记录当前迭代暂不处理、但需要在后续版本继续跟踪的事项。',
    steps: ['新建遗留项，或关联需求、用例、Bug 作为来源。', '指定类型、优先级和计划处理版本；计划处理版本可以是同一系统的其他版本。', '处理完成后在列表中更新状态，数据看板会同步统计待处理数量。'],
    flow: ['待处理', '已处理'],
    tip: '遗留项与需求、用例、Bug 一样需要遵守项目、系统与版本的一致性校验。',
  },
  {
    key: 'pending', index: '7', label: '待办事项',
    summary: '聚合当前项目中与你有关的 Bug 工作，帮助处理人和验证人快速完成下一步。',
    steps: ['在“待我处理”中查看分配给自己的待解决 Bug，填写修复说明并提交。', '在“待我验证”中验证已解决 Bug：通过则关闭，不通过则填写退回原因。', '在“我处理过”中回顾自己修复、验证或退回过的记录及流转历史。'],
    flow: ['待我处理', '待我验证', '我处理过'],
    flowDescription: '左侧导航红点只统计待我处理和待我验证，避免历史记录干扰当前工作。',
    tip: '待办按项目汇总，不受左下角系统与版本作用域影响。',
  },
  {
    key: 'dashboard', index: '8', label: '数据看板',
    summary: '通过数量卡片快速了解当前作用域内的测试资产和关键待处理数量。',
    steps: ['切换左下角系统和版本后，查看对应范围的统计。', '点击数量卡片打开明细，快速定位系统、版本、需求、用例、Bug 或遗留项。', '结合未关闭 Bug 数和待处理遗留项数，判断当前迭代风险。'],
    tip: '数据看板只展示统计与明细入口，不在此页修改业务数据。',
  },
]
</script>
