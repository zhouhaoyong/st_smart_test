# AGENTS.md

> AI 编程行为规范。覆盖编码准则、技术栈、后端/前端约定、UI 交互及工程约束。关注长期稳定的风格与工作方式，不描述业务。

---

## 沟通与确认

- 使用中文业务语言沟通，不暴露类名、字段名、组件名等技术细节。
- 先理解问题，再决定实现。
- 技术方案由 AI 负责，业务方向由用户确认。
- **用户提出的方案不一定合理；AI 须主动评估其在前端交互、后端逻辑等方面的可行性，若存在缺陷或可优化点，应提出质疑并给出更优建议，而非盲目执行。**
- 涉及布局、交互、数据结构变化时，先给出方案与影响范围，确认后再动手。

---

## 执行纪律（最高优先级）

> 本节约束 AI「怎么干活」，优先级高于下文所有业务/技术规范；与用户临时指令冲突时先提示再执行。

- **冲突即停、不确定即问**：当用户指令与本文件规则冲突，或需求存在多种合理解法、信息不足时，先指出并给出建议，等确认后再动手；禁止擅自假设、埋头就做。
- **不假装完成、不臆造**：禁止声称构建 / 测试 / 校验已通过而实际未执行；只完成一部分必须如实说明，不含糊带过；不编造文件、接口、字段或结果。
- **交付前自检**（每次收尾逐条核对，任一不满足则不算完成）：
  - 只改了与需求相关的文件，没有夹带无关改动；
  - 没有新建多余文件或 `_v2` / `_new` / `copy` / `temp` 版本文件；
  - 改动已通过静态校验（编译 / 语法），未实际运行的验证要如实说明；
  - 涉及接口响应、Toast、时间格式、软删除等，符合本文件对应约定；
  - 已用一句话说明「改了什么 / 影响范围 / 如何验证」。

---

## AI 编码准则（行为铁律）

**总则**
复用 > 重写 · 业务 > 技术 · 简单 > 优雅 · 稳定 > 重构 · 兼容 > 创新 · 局部修改 > 全局重构

**项目探索**
- 禁止全仓库递归搜索、全局索引、无限制批量读取；禁止读取第三方源码和无关目录。
- 按需读取 1~5 个相关文件，优先通过 CodeGraph、引用关系、类似实现定位。
- 定位与阅读顺序：业务代码 > 公共模块 > 基础组件，不逆序钻底层。
- 禁止重复读取相同文件。

**修改规则**
- 优先修改已有文件，不主动创建新文件；禁止创建 `_v2`、`_new`、`copy`、`temp` 等版本/临时文件。
- 不主动删除代码、文件或功能，删除前需确认影响范围。
- 不进行大范围格式化，不修改与当前需求无关的代码。
- 不为了抽象而抽象，不提前优化，不过度设计。
- 长流程任务分阶段推进，先输出方案，再逐步修改、边改边校验。

**模块与复用**
- 优先复用已有 Service、组件、样式、composable 和公共模块。
- 同一布局结构两处以上重复且无强业务差异时，才考虑抽取。
- 公共模块仅保留真实复用能力，禁止将业务逻辑塞入 common、core、base 等目录。

**兼容与稳定性**
- 不主动增加兼容分支，不默认兼容历史遗留实现。
- 不因代码"不够优雅"而重构稳定逻辑。
- 不主动引入新框架或复杂依赖。

**默认不执行**
- 默认不打开浏览器、不跑 Playwright / E2E、不 build、不 test，优先静态分析。

---

## 技术栈与环境

### 后端
- Python + FastAPI（全链路 async/await）
- SQLAlchemy 2.0 + MySQL
- 本地数据库：Docker 容器 `st_smart_test_mysql`，库 `st_smart_test`，用户 `root`，密码 `password`
- 排查数据库问题优先只读查询（查看数据与外键关系）

### 前端
- Vue3（script setup） + Element Plus + Pinia + Vite
- 优先复用现有技术栈，不增加新依赖

---

## 后端规范

- 默认软删除，日志和异常提示使用中文。
- 优先复用已有 Service，相似逻辑优先抽象。
- 单文件小于 800 行；新增/重构文件须达标，已超限的存量文件不因此强制拆分，仅在本就要重构时顺带收敛。

### 接口响应
- HTTP Status 永远固定为 200，禁止用其表达业务错误。
- 业务状态通过 `code` 字段表达：`200` 成功，`500` 服务器错误，`401` 鉴权错误等。
- `message` 为中文描述，`data` 返回实际数据（无数据时 `null`）。
- 禁止新增 `http_status`、`status_code` 等字段。
- 禁止所有响应都返回成功；错误场景必须返回对应错误码。

### 数据结构
- 约定好的 JSON 固定顶层键，不随意增减。
- 解析外部/AI 返回的 JSON 必须容错：缺键补空（空列表/空对象），类型不符归空，不因单字段缺失整体失败。
- 提供集中式的 `empty/normalize` 辅助函数，业务代码不各自处理。
- 不同用途的内容独立存储（如正文文本与结构化 JSON），不互相反向渲染或推导。
- 所有数据库表和字段都必须有中文名称/注释；仅字段名为 `id` 的主键可例外，外键及其他 ID 字段仍必须有中文名称。
- 新增表、字段，或修改现有表结构时，必须同步补充或维护对应的中文表名和字段名。

---

## 前端规范

### 布局与适配
- 禁止整页滚动，仅内容区域滚动；避免双滚动条。
- 页面主体可伸缩，内容区设置 `min-width: 0; min-height: 0`，防止撑出空白。
- 侧栏必须可访问全部菜单，空间不足时逐级收窄、自身滚动，不挤压主内容。
- 桌面端必须覆盖 1366、1440、1600、1920、2560 视口宽度（按视口宽度而非显示器尺寸判断断点）。
- 1366~1600 为紧凑桌面端：收窄侧栏、间距，筛选区可换行，但禁止降级为移动端布局。
- 不主动调整用户已确认的布局；确需变更，先说明方案与影响范围并确认后再动。

### 查询与分页
- 必须点击查询按钮才发起请求，不自动监听条件变化。
- 数据较多时启用分页，默认页大小 10，支持 10/50/100。
- 查询后回到第一页。

### Table
- 列表展示字段，必须和新建/编辑时的字段名称一致。
- 使用 Element Plus Table，表头禁止换行。
- 表格必须铺满可用宽度，禁止右侧大面积留白。
- 列宽按内容类型设置：短字段紧凑稳定，长字段最小宽度 + 省略号，时间列完整展示。
- 列确实无法容纳时，仅表格内部横向滚动，禁止页面整体横向滚动。
- 涉及表格列宽、固定列或空状态布局的改动，交付前必须分别验证有数据和无数据状态；不得只依据静态构建通过就交付，需确认数据行、空状态、固定操作列和表格宽度均正常。
- 表格上方的查询区、批量操作区可换行，但主操作按钮必须始终可见，禁止被挤出或裁剪。
- 操作列固定右侧，宽度按按钮数量设置，按钮不换行。
- 空状态统一复用现有组件，文案"暂无数据"。
- 结构固定为：查询/操作区 → 可伸缩表格区 → 固定分页区（分页不被推出可视区域）。

### Form
- 输入框宽度保持一致，优先复用已有表单布局。
- 必填项必须校验，错误提示以后端 `message` 为准。

### Dialog / Drawer
- 简单场景用 Dialog，复杂场景用 Drawer，保持现有交互风格。
- 弹窗宽高受视口约束：宽度不超过视口减边距，高度不超过可视区域；内容过长仅正文滚动。
- 紧凑桌面端下，弹窗内表单可从多列自动变单列，禁止字段或按钮被遮挡。
- 表格型弹窗的操作列固定右侧，表格滚动不得导致弹窗整体溢出。

### 页面组件化
- 单个 SFC 建议 ≤ 800 行、`<script setup>` 逻辑 ≤ 500 行；超出优先抽子组件 / composable，存量超限文件不强制拆分。
- 先复用现有页面和组件；两处以上相同布局且无强业务差异时才抽取。
- 筛选字段差异大时，复用样式和布局约束，不强行抽成通用表单。
- 已完成业务拆分的页面只补充布局能力，不重写业务组件。
- 公共逻辑抽离 composable；公共组件职责单一、插槽边界清晰，禁止放入业务接口、权限判断、字段定义等。

### AI 对话交互（流式）
> 适用于一切"用户输入 → 模型思考 → 模型输出"的流式界面。参考实现：`frontend/src/views/test-workbench/components/RequirementRefinementDrawer.vue`；交互参考 DeepSeek / 通义千问网页版。

- 通用头部：复制（左）· 收起/展开（右）。
- 用户输入提交后超过 1 行默认收起，溢出判定仅在收起态测量（展开态跳过，否则会误判）。
- 思考中默认展开并实时追加，思考结束自动收起、支持手动展开；模型不支持思考时不渲染思考区。
- 模型输出始终默认展开、支持收起；结构化 JSON 整轮解析完成后一次性呈现，不逐字流式（逐字流式仅用于思考）。
- 复制前提：该轮输出已结束（生成中不显示复制按钮）；走剪贴板 API 并降级，成功提示"已复制"。
- 滚动：默认吸底；用户手动上滑后暂停并显示"回到底部"按钮，滚回底部附近（阈值约 40px）恢复；提交新轮强制回底。
- 关闭弹窗时若本轮已调用过 LLM，需 ElMessageBox 二次确认，提示"本次对话不会保存"。
- 每次打开重新开始，不回放历史会话。

### Toast
- 错误只提示一次，禁止页面、axios、后端重复 toast。
- 内容以后端 `message` 为准。

### Button
- 主操作按钮唯一，删除操作必须二次确认。

### 时间
- 统一使用北京时间，统一展示格式，禁止展示 ISO 时间格式。

---

## Prompt 管理
- AI 功能提示词禁止硬编码，统一放在 `backend/docs/prompt/` 下。
- 使用 `.md` 文件，按功能命名（如 `generate_testcase.md`）。
- 业务代码只负责读取并传递 Prompt。

---

## AI 调用与耗时操作
- 耗时 AI 调用不放入高频瞬时接口；瞬时操作仅做轻量落库，不触发 AI。
- 耗时接口按需单独放宽超时（如 120s），不改全局默认（如 30s）。
- 落库前先预览：AI/耗时结果先展示确认，确认后再写入，不静默覆盖。
- 长流程有超时/卡顿风险时，按"快操作 + 按需重操作"拆分。

---

## Git 与危险操作
- 未经明确要求，禁止执行：commit、push、merge、rebase、创建 PR、修改 git 配置。
- 禁止：`git reset --hard`、`git clean -fd`、`git checkout -- .`、force push。
- 发现脏工作区不主动清理，只处理当前需求相关文件。
